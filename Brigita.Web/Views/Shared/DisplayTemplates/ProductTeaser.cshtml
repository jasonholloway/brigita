@using Brigita.View.Products
@model ProductTeaser
<div class="productTeaser">    
    <a href="@Url.MvcLink(Model.Link)">
        <h3>@Model.Name</h3>
        @if(Model.Image != null) {
            <img src="@Url.Content(Model.Image.Url)" />
        }
    </a>
</div>